// Idiomatic fixture: typed errors, no panics, no unsafe, no `as` casts on
// primitive numerics. A scan of this crate should produce zero findings.

pub mod calculator;
pub mod parser;
pub mod validator;
